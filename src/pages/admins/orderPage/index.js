import { memo } from "react";
import "./style.scss";
import { useNavigate } from "react-router-dom";
import { ROUTERS } from "utils/router";

const OrderPageAdPage = () => {
  return <div className="container">Order Page</div>;
};

export default memo(OrderPageAdPage);
